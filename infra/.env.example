# Generate both values before starting local infrastructure, for example with:
# openssl rand -base64 32
POSTGRES_PASSWORD=REPLACE_WITH_RANDOM_VALUE
REDIS_PASSWORD=REPLACE_WITH_RANDOM_VALUE
POSTGRES_PORT=5432
REDIS_PORT=6379
